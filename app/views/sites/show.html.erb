<div class="container">
<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @site.name %>
</p>

    <p>
        <strong>Site By:</strong>
        <%= which_user(@site.user_id).email %>
    </p>

<p>
  <strong>Category:</strong>
  <%= @site.category_id %>
</p>

<%= link_to 'Edit', edit_site_path(@site) %> |
<%= link_to 'Back', sites_path %>
</div>